<!DOCTYPE html>
<html lang="de">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>System Status</title>
  <style>
    body { font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif; background-color: #f4f4f9; margin: 0; padding: 20px; }
    h1 { text-align: center; color: #4CAF50; }
    .card-container { display: flex; flex-wrap: wrap; justify-content: center; gap: 20px; margin-top: 30px; }
    .card {
      background: white;
      padding: 20px;
      border-radius: 12px;
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
      width: 250px;
      text-align: center;
      transition: transform 0.2s;
    }
    .card:hover { transform: scale(1.05); }
    .card-title { font-size: 18px; color: #333; margin-bottom: 10px; }
    .card-value { font-size: 20px; font-weight: bold; color: #4CAF50; margin: 5px 0; }
    .section-title {
      text-align: center;
      font-size: 22px;
      margin-top: 60px;
      color: #333;
    }
    .back-link {
      display: block;
      text-align: center;
      margin-top: 40px;
      color: #4CAF50;
      text-decoration: none;
      font-size: 18px;
    }
    .back-link:hover { text-decoration: underline; }
  </style>
</head>
<body>
  <h1>🌱 System Status</h1>

  <!-- Statische Systemkarten -->
  <div class="card-container">
    <div class="card">
      <div class="card-title">🔢 Letzter Feuchtigkeitswert</div>
      <div class="card-value"><%= status.lastMoisture %></div>
    </div>
    <div class="card">
      <div class="card-title">🕛 Letzter Zeitstempel</div>
      <div class="card-value"><%= status.lastTimestamp %></div>
    </div>
    <div class="card">
      <div class="card-title">📂 Gesamtanzahl Einträge</div>
      <div class="card-value"><%= status.totalEntries %></div>
    </div>
    <div class="card">
      <div class="card-title">⏱️ Speicherintervall</div>
      <div class="card-value"><%= status.moistureSaveIntervalSec %> sek</div>
    </div>
    <div class="card">
      <div class="card-title">📅 Zusammenfassungsintervall</div>
      <div class="card-value"><%= status.moistureSummaryIntervalMin %> min</div>
    </div>
  </div>

  <!-- Dynamischer Sensorbereich -->
  <h2 class="section-title">📡 Aktive Sensorwerte</h2>
  <div class="card-container" id="sensor-cards-container">
    <!-- Wird durch JS befüllt -->
  </div>

  <a class="back-link" href="/">&larr; Zurück zur Übersicht</a>

  <script>
    async function fetchSensorData() {
      try {
        const res = await fetch('/api/sensor-status');
        const sensors = await res.json();

        const container = document.getElementById('sensor-cards-container');
        container.innerHTML = '';

        for (const [id, data] of Object.entries(sensors)) {
          const card = document.createElement('div');
          card.className = 'card';
          card.innerHTML = `
            <div class="card-title">Sensor ${id}</div>
            <div class="card-value">🌡️ ${data.temperature} °C</div>
            <div class="card-value">💧 ${data.humidity} %</div>
            <div class="card-value">🕛 ${new Date(data.timestamp).toLocaleString('de-DE')}</div>
          `;
          container.appendChild(card);
        }
      } catch (err) {
        console.error('Fehler beim Laden der Sensorwerte:', err);
      }
    }

    fetchSensorData();
    setInterval(fetchSensorData, 10_000); // alle 10 Sekunden aktualisieren
  </script>
</body>
</html>
